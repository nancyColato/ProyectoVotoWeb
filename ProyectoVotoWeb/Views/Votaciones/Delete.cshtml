@model ProyectoVotoWeb.Models.tblVotaciones

@{
    ViewData["Title"] = "Delete";
}

<div class="col-md-6 offset-md-3 col-sm-9 col-sm-1">
    <div class="card">
        <div class="card-header bg-danger">
            <h5 class="card-title text-center">Eliminar Voto</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-5 col-md-4">
                    @Html.DisplayNameFor(model => model.NumVotos)
                </dt>
                <dd class="col-sm-5">
                    @Html.DisplayFor(model => model.NumVotos)
                </dd>
                <dt class="col-sm-5 col-md-4">
                    @Html.DisplayNameFor(model => model.IdRestaurante)
                </dt>
                <dd class="col-sm-5 ">
                    @Html.DisplayFor(model => model.IdRestaurante)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="IdVoto" />
                <hr />
                <input type="submit" value="Eliminar" class="btn btn-danger" /> |
                <a asp-action="Index" class="btn btn-warning">Regresar!</a>
            </form>
        </div>

    </div>
</div>
